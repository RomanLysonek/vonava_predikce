Full-roadmap implementation notes (Tier A -> B -> C from the two-model review)
================================================================================
Source: chat discussion comparing ml/solution_final.py vs another_approach/,
then a rebuttal from the other_approach model, both reconciled and verified
against the actual data before starting. User chose "Tier A+B+C full roadmap".

Verified facts (don't re-derive, just cite):
- test_data.parquet has NO ProductAvailable column. Forecast is necessarily
  conditional on availability.
- train: 1060/51534 (2.06%) rows ProductAvailable=False. 268 of those have
  Quantity>0 (possible data noise / same-day partial availability).
- Per-product unavailable %: ProductId 24 ~22.0%, 26 ~10.3%, 23 ~9.9%,
  27 ~5.9%, 25 ~5.4%, 22 ~5.3%, 15 ~4.0%. Rest low/zero.
- Zero unavailable rows in the final 56 training days -> the recursive
  history *seed* for the real test forecast isn't directly contaminated;
  contamination mainly affects earlier training/CV windows.
- Contaminated vs availability-clean rolling-28 mean: 3.22% of available
  rows differ, mean abs diff 2.41 (Product 24 alone: 10.69).
- Calendar gaps (missing DATES, not ProductAvailable=False rows) exist for
  ProductId 1 (21 missing days) and ProductId 30 (8 missing days). Both
  gaps sit in the middle of otherwise-continuous, available history (data
  glitch, not a listing gap) -> reindex + treat as NaN/unknown, not zero.
- All 3 ML models (NeuralNet, XGBoost, LightGBM) share recursive_forecast_
  generic (ml/tree_worker.py:89, ml/solution_final.py:165-173) -> the NN's
  bad Christmas fold is NOT proven to be a recursion-specific effect (trees
  recurse too and did better) -- it's an open question (loss fn? capacity?
  single-seed CV vs 3-seed final? under a promo spike).
- CV currently trains/evals a SINGLE NN seed per fold (run_walk_forward_cv
  passes seed=cfg.seed only), but the final submission averages 3 seeds
  (cfg.seeds = 42,123,777). Reported CV "NeuralNet" row does not represent
  the actual submitted estimator. Must fix (Tier A item a3).
- Dynamic Ridge's cap (another_approach/notino_forecasting_solution.py:445)
  applies ONLY to Dynamic Ridge, NOT LightGBM (floor-only, :503) or any
  ensemble. Don't generalize this claim.

Plan tiers (see chat for full rationale/citations):
  Tier A = foundational correctness fixes, no architecture change
  Tier B = structural modeling wins (direct stacked panel, baseline-residual NN, Dynamic Ridge)
  Tier C = ablations, ensemble optimization, dashboard depth, stretch features

STATUS
------
[x] A1 reindex_daily_calendar (ml/framework.py) + wired into load_raw
[x] A2 availability-aware lag/rolling (add_train_lags, init_history, recursive
        history nanmean fallback, seasonal_naive_predict, moving_average_predict)
        + compute_baseline() weighted-same-weekday helper (also used by Tier B)
[x] A3 CV uses same 3-seed ensemble as final inference (run_walk_forward_cv)
[x] A4 fix discount_total (dropped naive sum; added effective_price_web/app)
[x] A5 compute_metrics: WAPE, sMAPE, RMSLE, Bias, BiasRatio (MAPE stays, demoted)
        + mean_fold (macro) vs global (micro) distinguished in summarize_oof
[x] A6 row-level OOF predictions saved (outputs/oof_predictions.parquet --
        origin, horizon, ProductId, DateKey, ProductAvailable, actual, baseline,
        per-seed + ensemble NN preds, XGBoost, LightGBM, naive baselines)
[x] A7 dev origins (12, broad/scattered) vs recent benchmark origins (last-4-
        folds) as two labeled, separate CV passes -- DEVELOPMENT_ORIGINS
        + recent_benchmark_origins() in ml/pipeline.py
[x] Update/add tests in tests/test_pipeline.py for all of the above (16 tests green)
[x] Restructured ml/ into framework.py (shared) + models/ (one file per model,
        neural_net/xgboost_model/lightgbm_model/naive_baselines) + pipeline.py
        (CV/training/export orchestrator); tree_worker.py is now a thin
        subprocess dispatcher. torch/xgboost process isolation verified intact.
[x] Tier A checkpoint: full test suite green (16/16), pipeline.py + tree_worker.py
    import smoke-tested, dashboard verified live (/ and /api/results both 200),
    git commit done before starting Tier B

[x] B1 stacked (ForecastOrigin x Horizon x ProductId) direct panel, kills
        recursion for NN/XGBoost/LightGBM -- build_direct_panel() +
        direct_panel_feature_names()/direct_panel_tree_frame() in
        ml/framework.py; models/neural_net.py gets a horizon embedding
        (QuantityNet) + predict_direct(); models/xgboost_model.py,
        lightgbm_model.py train/predict straight on the panel (horizon as
        a plain feature); tree_worker.py job protocol simplified to
        {train_panel, eval_panel} (no more history dict); pipeline.py's
        run_walk_forward_cv/run_final_forecast/run_final_tree_forecast
        rewritten around the panel, dead recursive code removed
        (recursive_forecast_generic, init_history, _nan_safe_window_stats,
        old feature_columns/tree_feature_frame). Caught + fixed 3 real
        bugs along the way: (1) TARGET_COVARIATE_COLUMNS leaking
        campaign_idx_web/app into direct_panel_feature_names, so trees
        saw those columns twice; (2) shift(-h) upcasting the int
        campaign_idx columns to float at series edges, which this
        xgboost rejects as category codes; (3) plain .astype("category")
        inferring per-DataFrame categories, so train/eval panels (built
        independently) disagreed on which product/campaign ids exist --
        fixed with a fixed cfg-derived category domain. Verified: full
        pytest suite (21/21) green, a reduced end-to-end smoke run
        (dev+holdout CV, final NN/tree/naive forecasts, JSON export) hit
        zero errors/NaNs, and the full real `python ml/pipeline.py` run
        (12 dev + 4 holdout origins x 3 seeds, final 3-seed ensemble)
        completed clean end-to-end (survived the machine sleeping mid-run)
        -- outputs/results.json regenerated, live dashboard confirmed
        serving it fresh (no restart needed, per server.py's per-request
        read). New recent-holdout numbers (README updated too): NeuralNet
        MAE 9.60/RMSE 13.89, XGBoost 7.85/12.26, LightGBM 7.79/11.86,
        SeasonalNaive 23.27/34.62, MovingAvg28 37.17/49.97; skill vs
        seasonal-naive (holdout, global MAE) +58.8%. Trees still edge out
        the NN on raw error (unsurprising/expected, see README) but the
        NN's skill margin over naive improved a lot vs the pre-B1 number
        (+47.8% -> +58.8%).
[x] B2 weighted same-weekday baseline as NN input feature + skip-connection
        residual target (predict log-residual, not raw log1p(Quantity)) --
        target_baseline column added to build_direct_panel() (framework.py),
        computed off the panel's own already-shifted seasonal_lag_{7,14,21,28}
        columns via a vectorized weighted-4:3:2:1 helper (_weighted_baseline),
        verified to agree exactly with the standalone compute_baseline()
        hist_df lookup for the same target dates. Given to the NN both as a
        plain input feature (numeric_feature_columns) and as the reference
        value its residual target is defined against: train_model now trains
        on residual_log1p_target() = log1p(target) - log1p(target_baseline)
        (models/neural_net.py), and predict_ensemble adds the baseline back
        (expm1(pred + log1p(baseline))) per seed at inference, before
        averaging across seeds in the natural (post-expm1) scale, not before.
        Trees get target_baseline as an extra input column only, no residual
        target (direct_panel_tree_frame already picks it up automatically via
        direct_panel_feature_names). Added 8 new tests (tests/test_pipeline.py,
        27/27 green): target_baseline schema + cross-check against
        compute_baseline, the residual formula, and 3 predict_ensemble
        skip-connection round-trip tests (perfect-residual exact target
        reconstruction, zero-residual -> baseline fallback, natural-scale-not
        -log-scale ensembling order). Also added timing instrumentation
        (ml/pipeline.py: time.perf_counter() checkpoints per CV fold (NN vs
        tree split) + per phase (dev CV, holdout CV, final NN, final trees,
        export), printed live and dumped to outputs/timings.json) ahead of
        the full run, since training time keeps growing release over
        release. Full `python ml/pipeline.py` run verified clean (0 NaN in
        the real final forecast; the pre-existing ~18% NaN rate in the
        earliest dev-CV fold's NN predictions is from the yearly
        seasonal_lag_364/365/371 features needing ~1y of history, present
        since Tier B1, NOT from target_baseline, which was 0% NaN in the same
        check) -- total wall time 3408s/56.8min (dev CV 1563s, holdout CV
        1229s, final NN ensemble 607s, final trees 8s). Holdout skill vs
        seasonal-naive (global MAE): +55.7%. NOTE: not a controlled A/B
        against the +58.8% Tier B1 number above -- recent_benchmark_origins is
        derived from train_raw's own max date, which has advanced since that
        run (the dataset keeps growing), so the two runs score different
        calendar weeks, not the same held-out fold.
[x] B3 Dynamic Ridge as 3rd model tier (Naive / Structured statistical /
        Global nonlinear) -- ml/models/dynamic_ridge.py using sklearn.Ridge
        + StandardScaler + OneHotEncoder; trained on baseline-relative log residual;
        configurable alpha/cap (Tier B repair).
[x] B4 common-population evaluation (Conditional Demand = primary) --
        summarize_oof refactored to report regimes (Realized/Conditional)
        and populations (Common/Specific) with detailed metrics (WAPE, Bias,
        Coverage, etc). Dashboard updated with toggle and Bias metrics.
        [REPAIRED] select_primary_summary bug fixed; common population
        enforced in legacy fold-level reports; subprocess timeouts added.
        [CLEANUP] Duplicate unbounded tests removed; build_direct_panel
        hardening (horizon check); holdout renamed to benchmark.

[ ] C1 recency weighting ablation grid (window x half-life)
[ ] C2 NN loss function ablation (Huber-residual / MSE-residual / combined)
[ ] C3 prediction-cap ablation (measure affected count, capped vs uncapped OOF)
[ ] C4 OOF-fitted ensemble weight search (dev-fit, holdout-checked once)
[ ] C5 dashboard: horizon curves, per-product error/bias, top-decile days,
        ablation showcase section
[ ] C6 stretch: dual web/app heads + effective price, holiday distance
        features, static results.json for GitHub Pages portability

Working file map (restructured post-Tier-A into framework.py + models/ + pipeline.py;
post-Tier-B1 the recursive engine is gone, replaced by the direct panel builder):
  ml/framework.py          - reindex, availability-aware features, baseline fn, metrics,
                             build_direct_panel()/direct_panel_feature_names()/
                             direct_panel_tree_frame() (Tier B1), model registry/metadata
  ml/models/neural_net.py  - NN model (QuantityNet w/ horizon embedding, train_model,
                             predict_direct)
  ml/models/xgboost_model.py, lightgbm_model.py - tree models, train/predict directly
                             on build_direct_panel()'s output (horizon = plain feature)
  ml/models/naive_baselines.py - seasonal-naive + moving-average
  ml/models/dynamic_ridge.py   - linear regression baseline w/ one-hot encoding,
                             trained on baseline-relative log residual (repaired)
  ml/pipeline.py           - CV loop (3-seed, dev/holdout split, OOF export) + final
                             forecasts, all panel-based (no recursion); NN residual
                             target (Tier B2) done; common-population evaluation
                             logic (B4 repair)
  ml/tree_worker.py        - thin subprocess dispatcher over models/xgboost_model.py,
                             lightgbm_model.py, and dynamic_ridge.py
  tests/test_pipeline.py   - update + extend
  webapp/static/*          - Tier A5 metric surfacing, Tier C5 dashboard depth
